<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="custom-module.list">
      <field name="name">custom-module list</field>
      <field name="model">custom-module.custom-module</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="custom-module.action_window">
      <field name="name">custom-module window</field>
      <field name="res_model">custom-module.custom-module</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="custom-module.action_server">
      <field name="name">custom-module server</field>
      <field name="model_id" ref="model_custom-module_custom-module"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="custom-module" id="custom-module.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="custom-module.menu_1" parent="custom-module.menu_root"/>
    <menuitem name="Menu 2" id="custom-module.menu_2" parent="custom-module.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="custom-module.menu_1_list" parent="custom-module.menu_1"
              action="custom-module.action_window"/>
    <menuitem name="Server to list" id="custom-module" parent="custom-module.menu_2"
              action="custom-module.action_server"/>
    -->
  </data>
</odoo>